pipeline {
    agent any
    stages {
        stage('Build with jenkinsfile demo') {
            steps {
                echo 'Hello, Build completed from Jenkinsfile!'
            }
        }
    }
}
